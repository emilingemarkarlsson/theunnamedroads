name: Project Stage + Process Sync

on:
  issues:
    types: [opened, reopened]
  pull_request:
    types: [opened, reopened, ready_for_review, closed]
  deployment_status:
    types: [created]

permissions:
  contents: read
  issues: read
  pull-requests: read
  repository-projects: write

env:
  PROJECT_OWNER: emilingemarkarlsson
  PROJECT_NUMBER: "6"

  STAGE_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFA4w
  STAGE_REDO_AGENT: 5812ea14
  STAGE_IN_AGENT: 419ecf79
  STAGE_REDO_APPROVAL: 77c32be4
  STAGE_APPROVED_WAITING_RELEASE: e2b0ddce
  STAGE_LIVE: 9d6f61dc

  PROCESS_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFKM4
  PROCESS_AGENT_READY: f41ce141
  PROCESS_IN_AGENT: a08d452f
  PROCESS_AGENT_REVIEW: 02942ecd
  PROCESS_APPROVED: d55e0c9d
  PROCESS_LIVE: 63cb9dc3
  PROCESS_BLOCKED: 82ad5fe3

  PROJECT_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFA40
  PROJECT_THA: 1636c0e3
  PROJECT_TUR: 3cb37b1b
  PROJECT_TAF: 9f73d059
  PROJECT_THB: ec97657e
  PROJECT_AAF: 518b7357
  PROJECT_TAN: 72683752
  PROJECT_TOL: 23de942f
  PROJECT_TSL: 3cb44d62

  PRIORITY_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFBU8
  PRIORITY_P2: a2704f9f

  WORKTYPE_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFBV8
  WORKTYPE_BUILD: 8afca584

  HANDSHAKE_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFMCI
  HANDSHAKE_ACCEPTED: 86fc959f
  HANDSHAKE_BLOCKED: fe94f13f

  MODEL_LANE_FIELD_ID: PVTSSF_lAHOAQ4nb84BkT_9zhjFMBM
  MODEL_EVERYDAY: 7eb4529e
  MODEL_MEDIUM: a121b0b3
  MODEL_HARD: 1edf780f

jobs:
  sync-stage:
    runs-on: ubuntu-latest
    steps:
      - name: Update project stage and process
        uses: actions/github-script@v7
        env:
          PROJECT_AUTOMATION_TOKEN: ${{ secrets.PROJECT_AUTOMATION_TOKEN }}
        with:
          github-token: ${{ env.PROJECT_AUTOMATION_TOKEN != '' && env.PROJECT_AUTOMATION_TOKEN || github.token }}
          script: |
            const owner = process.env.PROJECT_OWNER;
            const projectNumber = Number(process.env.PROJECT_NUMBER);

            const stageFieldId = process.env.STAGE_FIELD_ID;
            const processFieldId = process.env.PROCESS_FIELD_ID;
            const projectFieldId = process.env.PROJECT_FIELD_ID;
            const priorityFieldId = process.env.PRIORITY_FIELD_ID;
            const workTypeFieldId = process.env.WORKTYPE_FIELD_ID;
            const handshakeFieldId = process.env.HANDSHAKE_FIELD_ID;
            const modelLaneFieldId = process.env.MODEL_LANE_FIELD_ID;

            const stageByKey = {
              REDO_AGENT: process.env.STAGE_REDO_AGENT,
              IN_AGENT: process.env.STAGE_IN_AGENT,
              REDO_APPROVAL: process.env.STAGE_REDO_APPROVAL,
              APPROVED_WAITING_RELEASE: process.env.STAGE_APPROVED_WAITING_RELEASE,
              LIVE: process.env.STAGE_LIVE,
            };

            const processByKey = {
              AGENT_READY: process.env.PROCESS_AGENT_READY,
              IN_AGENT: process.env.PROCESS_IN_AGENT,
              AGENT_REVIEW: process.env.PROCESS_AGENT_REVIEW,
              APPROVED: process.env.PROCESS_APPROVED,
              LIVE: process.env.PROCESS_LIVE,
              BLOCKED: process.env.PROCESS_BLOCKED,
            };

            const projectByRepo = {
              "tha-website": process.env.PROJECT_THA,
              "theunnamedroads": process.env.PROJECT_TUR,
              "tur-theagentfabric": process.env.PROJECT_TAF,
              "thb-site": process.env.PROJECT_THB,
              "tur-aaf": process.env.PROJECT_AAF,
              "tan-site": process.env.PROJECT_TAN,
              "tol-website": process.env.PROJECT_TOL,
              "tsl-website": process.env.PROJECT_TSL,
            };

            async function getProjectId() {
              const query = `
                query($owner: String!, $number: Int!) {
                  user(login: $owner) {
                    projectV2(number: $number) { id }
                  }
                }
              `;
              const res = await github.graphql(query, { owner, number: projectNumber });
              return res.user.projectV2.id;
            }

            async function upsertItem(projectId, contentId) {
              const listQuery = `
                query($projectId: ID!) {
                  node(id: $projectId) {
                    ... on ProjectV2 {
                      items(first: 100) {
                        nodes {
                          id
                          content {
                            __typename
                            ... on Issue { id }
                            ... on PullRequest { id }
                          }
                        }
                      }
                    }
                  }
                }
              `;
              const listRes = await github.graphql(listQuery, { projectId });
              const existing = listRes.node.items.nodes.find((n) => n?.content?.id === contentId);
              if (existing) return existing.id;

              const addMutation = `
                mutation($projectId: ID!, $contentId: ID!) {
                  addProjectV2ItemById(input: {projectId: $projectId, contentId: $contentId}) {
                    item { id }
                  }
                }
              `;
              const addRes = await github.graphql(addMutation, { projectId, contentId });
              return addRes.addProjectV2ItemById.item.id;
            }

            async function setSingleSelect(projectId, itemId, fieldId, optionId) {
              if (!optionId) return;
              const mutation = `
                mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $optionId: String!) {
                  updateProjectV2ItemFieldValue(
                    input: {
                      projectId: $projectId,
                      itemId: $itemId,
                      fieldId: $fieldId,
                      value: { singleSelectOptionId: $optionId }
                    }
                  ) {
                    projectV2Item { id }
                  }
                }
              `;
              await github.graphql(mutation, { projectId, itemId, fieldId, optionId });
            }

            function classifyModelLane(payload) {
              if (context.eventName === "issues") return process.env.MODEL_EVERYDAY;
              if (context.eventName === "pull_request") {
                const draft = !!payload.pull_request?.draft;
                return draft ? process.env.MODEL_EVERYDAY : process.env.MODEL_MEDIUM;
              }
              if (context.eventName === "deployment_status") return process.env.MODEL_HARD;
              return null;
            }

            function statusFromEvent() {
              if (context.eventName === "issues") {
                if (["opened", "reopened"].includes(context.payload.action)) {
                  return {
                    stage: stageByKey.REDO_AGENT,
                    process: processByKey.AGENT_READY,
                    handshake: process.env.HANDSHAKE_ACCEPTED,
                  };
                }
              }

              if (context.eventName === "pull_request") {
                if (["opened", "reopened"].includes(context.payload.action)) {
                  return {
                    stage: stageByKey.IN_AGENT,
                    process: processByKey.IN_AGENT,
                    handshake: process.env.HANDSHAKE_ACCEPTED,
                  };
                }
                if (context.payload.action === "ready_for_review") {
                  return {
                    stage: stageByKey.REDO_APPROVAL,
                    process: processByKey.AGENT_REVIEW,
                    handshake: process.env.HANDSHAKE_ACCEPTED,
                  };
                }
                if (context.payload.action === "closed" && context.payload.pull_request?.merged) {
                  return {
                    stage: stageByKey.APPROVED_WAITING_RELEASE,
                    process: processByKey.APPROVED,
                    handshake: process.env.HANDSHAKE_ACCEPTED,
                  };
                }
              }

              if (context.eventName === "deployment_status") {
                const state = (context.payload.deployment_status?.state || "").toLowerCase();
                if (state === "success") {
                  return {
                    stage: stageByKey.LIVE,
                    process: processByKey.LIVE,
                    handshake: process.env.HANDSHAKE_ACCEPTED,
                  };
                }
                if (state === "failure" || state === "error") {
                  return {
                    stage: stageByKey.IN_AGENT,
                    process: processByKey.BLOCKED,
                    handshake: process.env.HANDSHAKE_BLOCKED,
                  };
                }
              }

              return null;
            }

            async function contentIdsFromEvent() {
              if (context.eventName === "issues") {
                return context.payload.issue?.node_id ? [context.payload.issue.node_id] : [];
              }

              if (context.eventName === "pull_request") {
                return context.payload.pull_request?.node_id ? [context.payload.pull_request.node_id] : [];
              }

              if (context.eventName === "deployment_status") {
                const sha = context.payload.deployment?.sha;
                if (!sha) return [];

                const prs = await github.rest.repos.listPullRequestsAssociatedWithCommit({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  commit_sha: sha,
                });
                return prs.data.map((pr) => pr.node_id).filter(Boolean);
              }

              return [];
            }

            const mapped = statusFromEvent();
            if (!mapped) {
              core.info("No transition for this event.");
              return;
            }

            const projectId = await getProjectId();
            const contentIds = await contentIdsFromEvent();
            if (contentIds.length === 0) {
              core.info("No issue/PR content IDs found.");
              return;
            }

            const repoProjectOptionId = projectByRepo[context.repo.repo];
            const modelLaneOptionId = classifyModelLane(context.payload);

            for (const contentId of contentIds) {
              const itemId = await upsertItem(projectId, contentId);
              await setSingleSelect(projectId, itemId, stageFieldId, mapped.stage);
              await setSingleSelect(projectId, itemId, processFieldId, mapped.process);
              await setSingleSelect(projectId, itemId, projectFieldId, repoProjectOptionId);
              await setSingleSelect(projectId, itemId, priorityFieldId, process.env.PRIORITY_P2);
              await setSingleSelect(projectId, itemId, workTypeFieldId, process.env.WORKTYPE_BUILD);
              await setSingleSelect(projectId, itemId, handshakeFieldId, mapped.handshake);
              await setSingleSelect(projectId, itemId, modelLaneFieldId, modelLaneOptionId);
              core.info(`Synced item ${itemId}`);
            }
