# Agent run notes

Short durable notes promoted from `learn:candidate` when they are not yet a standing rule.
Prefer updating `.cursor/rules/` or `.cursor/skills/` first (see `promote-learning` skill).
